<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraintLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".SearchActivity$PlaceholderFragment">

    <SearchView
        android:id="@+id/sv_input_tuyen_bus"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="20dp"
        android:background="@drawable/corner_bg2"
        android:iconifiedByDefault="false"
        android:queryHint="Tìm kiếm tuyến buýt...">

        <requestFocus />
    </SearchView>

    <ListView
        android:id="@+id/lv_bus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/listview_item"></ListView>

</LinearLayout>